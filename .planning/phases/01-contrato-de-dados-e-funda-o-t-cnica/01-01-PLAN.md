---
phase: 01-contrato-de-dados-e-funda-o-t-cnica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/types/editor-music.ts
autonomous: true
requirements:
  - DATA-01
user_setup: []
must_haves:
  truths:
    - "Dependência vexflow declarada e tipos LeafMusic exportados com schemaVersion e variant"
  artifacts:
    - "package.json contém vexflow"
    - "src/types/editor-music.ts existe e exporta LEAF_MUSIC_BLOCK_TYPE_SPIKE e LeafMusicSpikeData"
  key_links:
    - "Tipos referenciados pelo plano 01-02 para save()/render()"
---

<objective>
Instalar **vexflow** (v5) e criar **`src/types/editor-music.ts`** com o contrato JSON (`schemaVersion`, `variant`, payload da spike) e documentação dos **limites MVP** (D-07), alinhado a **D-01–D-03** do CONTEXT.
Output: dependências prontas + fonte única de verdade para serialização dos blocos musicais.
</objective>

<execution_context>
@C:/Users/07404626102/ProjetoExtensao/leaf-sound/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/07404626102/ProjetoExtensao/leaf-sound/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-contrato-de-dados-e-funda-o-t-cnica/01-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Instalar vexflow ^5</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json</read_first>
  <action>Executar no diretório raiz do projeto: `npm install vexflow@^5.0.0` (dependência de runtime, não dev). Não remover dependências existentes.</action>
  <verify>npm ls vexflow</verify>
  <acceptance_criteria>
    - "package.json contém a linha com \"vexflow\" (nome exato do pacote)"
    - "package-lock.json existe e lista vexflow após instalação"
  </acceptance_criteria>
  <done>vexflow instalado e listável</done>
</task>

<task type="auto">
  <name>Definir tipos e constantes leafMusic (spike)</name>
  <files>src/types/editor-music.ts</files>
  <read_first>.planning/phases/01-contrato-de-dados-e-funda-o-t-cnica/01-CONTEXT.md, src/lib/storage.ts</read_first>
  <action>Criar arquivo `src/types/editor-music.ts` que exporte:
- Constante string `LEAF_MUSIC_SCHEMA_VERSION = 1` (número ou literal conforme padrão do projeto; se número, usar `as const` onde fizer sentido).
- Constante `LEAF_MUSIC_BLOCK_TYPE_SPIKE = 'leafMusic_spike'` (exatamente este string — D-01).
- Tipo `LeafMusicVariant = 'spike' | 'chord' | 'tab' | 'staff'` (D-02).
- Interface `LeafMusicSpikeData` com campos obrigatórios: `schemaVersion: 1`, `variant: 'spike'`, e um campo `payload` (objeto ou string) suficiente para a spike — sugerido: `payload: { demo: string }` com default `demo: 'spike-v1'` para prova de round-trip.
- Função `isLeafMusicSpikeData(data: unknown): data is LeafMusicSpikeData` validando `schemaVersion === 1`, `variant === 'spike'`, e presença de `payload`.
- Comentário JSDoc no topo ou acima de `LeafMusicSpikeData` documentando **limites MVP orientativos** (D-07): ex. tab curta ~4 compassos em fases futuras; spike não enforce — apenas orientação para implementações posteriores.

Não importar vexflow neste arquivo (apenas tipos TS puros).</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - "src/types/editor-music.ts contém a substring exata 'leafMusic_spike'"
    - "src/types/editor-music.ts contém 'schemaVersion'"
    - "src/types/editor-music.ts contém função isLeafMusicSpikeData"
    - "src/types/editor-music.ts contém comentário com a palavra 'MVP' ou 'compassos' (documentação D-07)"
  </acceptance_criteria>
  <done>Tipos compilam e cobrem contrato DATA-01</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint` passa
- [ ] `src/types/editor-music.ts` criado conforme critérios
</verification>

<success_criteria>
- vexflow presente em package.json
- Tipos exportados e guard implementado
- Nenhuma alteração fora do escopo (sem tool ainda)
</success_criteria>
