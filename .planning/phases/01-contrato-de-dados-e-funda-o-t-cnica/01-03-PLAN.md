---
phase: 01-contrato-de-dados-e-funda-o-t-cnica
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/components/TextEditor/index.tsx
  - src/app/editor/editor-styles.css
autonomous: true
requirements:
  - DATA-01
user_setup: []
must_haves:
  truths:
    - "Usuário pode inserir bloco spike pelo Editor, salvar nota e recarregar sem perder o bloco"
  artifacts:
    - "TextEditor registra tool leafMusic_spike"
  key_links:
    - "Fluxo /editor?id= + storage existente valida DATA-01 end-to-end"
---

<objective>
Registrar **`LeafMusicSpikeTool`** no **`TextEditor`** com **dynamic import** (D-10), ajustar estilos mínimos para o bloco musical não “quebrar” o layout (alinhado ao prose existente), e **validar** o success criteria do roadmap: round-trip salvar → recarregar na rota `/editor`.
Output: Fase 1 executável e verificável manualmente + build verde.
</objective>

<execution_context>
@C:/Users/07404626102/ProjetoExtensao/leaf-sound/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/07404626102/ProjetoExtensao/leaf-sound/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/TextEditor/index.tsx
@src/app/editor/page.tsx
@src/lib/editorjs/tools/LeafMusicSpikeTool.ts
@.planning/phases/01-contrato-de-dados-e-funda-o-t-cnica/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Registrar tool leafMusic_spike no TextEditor</name>
  <files>src/components/TextEditor/index.tsx</files>
  <read_first>src/components/TextEditor/index.tsx, src/lib/editorjs/tools/LeafMusicSpikeTool.ts</read_first>
  <action>No `initEditor` async dentro de `TextEditor`, após os imports existentes do Editor.js, adicionar dynamic import da tool:
- `const LeafMusicSpikeTool = (await import('@/lib/editorjs/tools/LeafMusicSpikeTool')).default`
No objeto `tools` do `new EditorJS({...})`, adicionar entrada com chave **`leafMusic_spike`** (string exata — deve coincidir com `LEAF_MUSIC_BLOCK_TYPE_SPIKE` em tipos):
```ts
leafMusic_spike: {
  class: LeafMusicSpikeTool,
  shortcut?: undefined,
},
```
Garantir que `initialData` continua a ser passado e que `onChange` dispara save como hoje. Não remover tools existentes (header, paragraph, list, code).

Se o projeto usar path alias `@/lib`, manter consistência com outros imports em `TextEditor`.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - "src/components/TextEditor/index.tsx contém a substring 'leafMusic_spike'"
    - "src/components/TextEditor/index.tsx contém 'LeafMusicSpikeTool' ou import path para tools/LeafMusicSpikeTool"
  </acceptance_criteria>
  <done>Tool registrada</done>
</task>

<task type="auto">
  <name>Estilos mínimos para bloco musical</name>
  <files>src/app/editor/editor-styles.css</files>
  <read_first>src/app/editor/editor-styles.css, src/components/TextEditor/index.tsx</read_first>
  <action>Adicionar regras CSS para `.leaf-music-spike` (ou classe usada na tool): largura máxima 100%, overflow-x auto se necessário, margem vertical consistente com blocos Editor.js. Não alterar tema global além do necessário.</action>
  <verify>grep leaf-music editor-styles.css</verify>
  <acceptance_criteria>
    - "editor-styles.css contém 'leaf-music' ou a classe exata usada no wrapper da tool"
  </acceptance_criteria>
  <done>Bloco não estoura layout em viewport comum</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Editor na rota /editor com nova opção de bloco musical spike; notação VexFlow visível após inserir.</what-built>
  <how-to-verify>1) `npm run dev` 2) Abrir uma nota com `?id=` válido (ou fluxo que crie nota) 3) Adicionar bloco "Spike musical" / título da toolbox 4) Esperar autosave (ou disparar mudança) 5) Recarregar a página 6) Confirmar: bloco ainda presente, JSON intacto (sem erro vermelho no bloco), consola sem erro no fluxo feliz.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] Checklist manual em 01-VALIDATION.md cumprido
</verification>

<success_criteria>
- DATA-01 verificado: serialização + render pós-reload
- Roadmap success criteria 1–3 endereçados (tipos + spike + limites em comentário tipo)
</success_criteria>
