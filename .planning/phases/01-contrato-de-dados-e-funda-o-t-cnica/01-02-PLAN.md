---
phase: 01-contrato-de-dados-e-funda-o-t-cnica
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/editorjs/tools/LeafMusicSpikeTool.ts
autonomous: true
requirements:
  - DATA-01
user_setup: []
must_haves:
  truths:
    - "Bloco spike renderiza notação VexFlow em SVG e save() devolve LeafMusicSpikeData válido"
  artifacts:
    - "src/lib/editorjs/tools/LeafMusicSpikeTool.ts existe"
  key_links:
    - "save() produz JSON compatível com storage Note.content"
---

<objective>
Implementar a **custom tool** Editor.js `LeafMusicSpikeTool`: montar DOM no `render()`, desenhar **uma pauta mínima** (clave + 1 nota ou trecho demo) com **VexFlow 5** em **SVG** (D-04/D-05), implementar `save()` retornando dados alinhados a `LeafMusicSpikeData`, e **estado de erro** no bloco se dados inválidos (D-11).
Output: tool pronta para registro no `TextEditor` (plano 01-03).
</objective>

<execution_context>
@C:/Users/07404626102/ProjetoExtensao/leaf-sound/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/07404626102/ProjetoExtensao/leaf-sound/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contrato-de-dados-e-funda-o-t-cnica/01-CONTEXT.md
@src/types/editor-music.ts
@node_modules/vexflow/package.json
</context>

<tasks>

<task type="auto">
  <name>Implementar LeafMusicSpikeTool (BlockTool)</name>
  <files>src/lib/editorjs/tools/LeafMusicSpikeTool.ts</files>
  <read_first>src/types/editor-music.ts, src/components/TextEditor/index.tsx, .planning/phases/01-contrato-de-dados-e-funda-o-t-cnica/01-RESEARCH.md</read_first>
  <action>Criar `src/lib/editorjs/tools/LeafMusicSpikeTool.ts` com **export default** de uma classe compatível com Editor.js 2.x:

1. **Constructor** aceita o objeto de dados do Editor.js (ex.: propriedade `data`). Se ausente ou inválido após guard, usar default: `{ schemaVersion: 1, variant: 'spike', payload: { demo: 'spike-v1' } }` importando constantes/tipos de `@/types/editor-music` (ajustar alias se necessário).

2. **static get toolbox()**: retornar `title` em **português**, ex.: "Spike musical (teste)" ou "Notação (teste)", e `icon` SVG inline simples (nota ou símbolo musical).

3. **render()**: criar um `div` wrapper com classe CSS clara (ex.: `leaf-music-spike`) e largura 100%. Dentro, criar um filho `div` com **id único** por instância (ex.: `leaf-music-spike-${crypto.randomUUID?.() ?? Date.now()}`) para o renderer VexFlow. Importar **dinamicamente** `vexflow` apenas dentro de `render()` ou de função chamada por ele (evitar execução no SSR do módulo). Usar **Factory** ou API documentada em VexFlow 5 para desenhar **pelo menos uma pauta com clave de sol** e **uma nota** (ou sistema mínimo visível). Preferir **SVG** (D-05). Se `loadFonts`/`setFonts` for assíncrono, aguardar com `.then()` e então `factory.draw()`, atualizando o DOM; se falhar, mostrar texto em pt-BR no bloco (D-11).

4. **save()**: retornar o objeto `LeafMusicSpikeData` atual (mesmo referência lógica que em `this.data`), garantindo `schemaVersion: 1` e `variant: 'spike'`.

5. **destroy()** (se suportado pela API Editor.js): limpar container / referências se necessário.

6. Se `isLeafMusicSpikeData` falhar em dados vindos do disco: ainda renderizar wrapper com mensagem curta **"Dados do bloco inválidos"** (pt-BR) em vez de throw.

Use apenas TypeScript; `@ts-expect-error` mínimo se tipos do Editor.js não exportarem tool class claramente.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - "LeafMusicSpikeTool.ts contém export default"
    - "LeafMusicSpikeTool.ts importa de '@/types/editor-music' ou 'src/types/editor-music' path consistente com tsconfig"
    - "LeafMusicSpikeTool.ts contém a substring 'toolbox'"
    - "LeafMusicSpikeTool.ts contém 'vexflow' (import dinâmico ou estático apenas dentro de função client-safe)"
    - "LeafMusicSpikeTool.ts contém 'save' e retorno alinhado a schemaVersion"
  </acceptance_criteria>
  <done>Tool implementada e lint OK</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint`
- [ ] `npm run build` (após tool estável)
</verification>

<success_criteria>
- Renderização visível no DOM após montar o bloco (validação manual no plano 01-03)
- save() serializa dados compatíveis com tipos
</success_criteria>
